<%= form_for(@vehicle, local: true) do |f| %>
    <ons-list modifier="inset">
      <ons-list-header>Vehicle Information</ons-list-header>
      <ons-list-item modifier="longdivider"><%= f.text_field :name, autocomplete: "name", class: 'text-input', placeholder: 'Vehicle Nickname' %></ons-list-item>
      <ons-list-item modifier="longdivider"><%= f.text_field :color, autocomplete: "color", class: 'text-input', placeholder: 'Color' %></ons-list-item>
    </ons-list>
    <br>
    <ons-list modifier="inset">
      <ons-list-item modifier="longdivider"><%= f.text_field :make, autocomplete: "make", class: 'text-input', placeholder: 'Make' %></ons-list-item>
      <ons-list-item modifier="longdivider"><%= f.text_field :model, autocomplete: "model", class: 'text-input', placeholder: 'Model' %></ons-list-item>
      <ons-list-item modifier="longdivider"><%= f.text_field :year, autocomplete: "year", class: 'text-input', placeholder: 'Year' %></ons-list-item>
    </ons-list>
    <br>
    <ons-list modifier="inset">
      <ons-list-item modifier="longdivider"><i class="far fa-question-circle" style= font-size:0.8rem;
            onclick="ons.notification.toast('The VIN (vehicle identification number) can often be found on the lower-left corner of the dashboard, in front of the steering wheel. You can read the number by looking through the windshield.', { timeout: 3000})">
            </i><%= @vehicle.vin %><%= f.text_field :vin, autocomplete: "vin", class: 'text-input', placeholder: '  VIN#' %></ons-list-item>
      <ons-list-item modifier="longdivider"><%= f.text_field :license_plate, autocomplete: "license_plate", class: 'text-input', placeholder: 'License Plate #' %></ons-list-item>
      <ons-list-item modifier="longdivider" style="color:grey;">Date of Last Inspection: <%= f.date_field :inspection_date, autocomplete: "inspection_date", class: 'text-input', placeholder: 'Date of last Inspection' %></ons-list-item>
    </ons-list>
    <div style="display:block; text-align: center; margin-top: 10px;">
        <p id="upload_msg" >Upload a picture of your vehicle</p>
        <i id="check_mark" class="fa fa-check-circle" style="font-size:36px;color:blue"></i>  
        <i class="fa fa-camera button" id="camera_button" style="margin:15px; background-color: white; color:#3A405A; "></i>
   </div>
    <%= f.file_field :avatar, multiple: false,
                  direct_upload: true, accept:"image/*", capture:"camera", style:"margin:10px; font-size: 1.2rem;"%>
    <div class="actions" style="text-align: center; margin-top: 30px; margin-bottom: 30px;">
      <%= f.submit "Submit", class: "button", style:"background-color: white; color:#3A405A; font-weight: bold;" %>
    </div>
<% end %> 
<style>
#vehicle_avatar{
  display:none;
}
#check_mark{
  display: none;
}
</style>
<script>
  var camera_button = document.getElementById("camera_button")
    camera_button.addEventListener('click',function(e){
        var vehicle_image_upload = document.getElementById("vehicle_avatar")
        vehicle_image_upload.click();

       
    })

     
    var vehicle_image_input= document.getElementById("vehicle_avatar")
    vehicle_image_input.addEventListener('change', function(e){
        if (vehicle_image_input.files.length >0){
          var check_mark = document.getElementById("check_mark").style.display="block"
          var upload_msg = document.getElementById("upload_msg").style.display ="none"
        }
    })
</script>